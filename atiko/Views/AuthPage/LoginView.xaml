<UserControl
    x:Class="atiko.Views.LoginView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Grid Background="#F7F2EE" MinWidth="480" Padding="32" HorizontalAlignment="Stretch">
        <StackPanel Spacing="18" HorizontalAlignment="Stretch">
            <TextBlock Text="Email" FontSize="18" FontWeight="SemiBold" Foreground="#62473D" />
            <TextBox
                x:Name="EmailTextBox"
                PlaceholderText="email@example.com"
                Text="{Binding Email, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                TextChanged="EmailTextBox_TextChanged"
                Background="White"
                BorderBrush="#C6B1A1"
                BorderThickness="1"
                CornerRadius="8"
                FontSize="16"
                Height="48"
                Padding="12"
                HorizontalAlignment="Stretch"
                MinWidth="480"
                VerticalContentAlignment="Center"/>
            <TextBlock Text="Password" FontSize="18" FontWeight="SemiBold" Foreground="#62473D" />
            <PasswordBox
                x:Name="LoginPasswordBox"
                PasswordChanged="LoginPasswordBox_PasswordChanged"
                PlaceholderText="Enter password"
                Background="White"
                BorderBrush="#C6B1A1"
                BorderThickness="1"
                CornerRadius="8"
                FontSize="16"
                Height="48"
                Padding="12"
                HorizontalAlignment="Stretch"
                MinWidth="480"
                VerticalContentAlignment="Center"/>
            <Button
                Content="Log In"
                Command="{Binding LoginCommand}"
                IsEnabled="{Binding CanLogin}"
                Style="{StaticResource PrimaryButtonStyle}"
                HorizontalAlignment="Stretch"
                MinWidth="480"/>
        </StackPanel>
    </Grid>
</UserControl>